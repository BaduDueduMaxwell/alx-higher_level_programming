#!/usr/bin/python3
def max_integer(my_list=[0]):
    for num in range(my_list[0]):
        highest_num = my_list[0]
        if num > highest_num:
            highest_num = num
            return highest_num
        else:
            return (len(0, None))
